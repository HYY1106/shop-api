package com.fh.shop.api.recipient.controller;

/*@RestController
@RequestMapping("/api/recipient")
@Api(tags = "收件人接口")*/
public class RecipientController {
/*
    @Resource(name = "recipientService")
    private RecipientService recipientService;


    //查询收件人   作废
    @GetMapping("/list")
    @Check
    @ApiOperation("获取会员对应的所有收件人列表")
    @ApiImplicitParams({
            @ApiImplicitParam(name = "x-auth",value = "头信息",type = "string",required = true,paramType = "header")
    })
    public ServerResponse list(HttpServletRequest request){
      MemberVo member = (MemberVo) request.getAttribute(SystemConstant.CURR_MEMBER);
      Long memberId = member.getId();
      return recipientService.findList(memberId);
    }*/
}
